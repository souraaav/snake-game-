name: ☕ Java CI – Snake Game (No Maven)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-run:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v3

    - name: ☕ Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: 🧹 Create build directory
      run: mkdir -p out

    - name: 📦 Compile Java files
      run: javac -d out $(find . -name "*.java")

    - name: 🚀 Run the game (optional)
      run: |
        echo "Compiled successfully. You can add a test or main class to run here."
